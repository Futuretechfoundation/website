{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "api/registry.js",  // Adjust if necessary
  "dependencies": {
    "mongodb": "^5.6.0"
  },
  "scripts": {
    "start": "node api/registry.js"
  }
}
